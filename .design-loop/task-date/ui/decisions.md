# 时间弹窗决定

复用TaskStore事务与独立Todo副本，新增组件仅产生用户选择。保留旧卡片日期入口和右键菜单；本次只替换底部任务入口。上下文工具栏加入日历以保留已有删除语义。

按截图改为中心“时间”、纵向图标快捷项和4周网格；顶部无外圈、面板固定300pt，系统popover承载。第一轮实机后调深灰底色和文字尺寸；选择日期高亮，父popover关闭后按钮解除选中。

本地提醒与系统队列分步：先保存，再主动授权；权限拒绝保留时间并提示。既有Things来源metadata不启用，手动复制清提醒避免重复弹。仅QA改变数据，截图存会话证据而不复制到源码。
