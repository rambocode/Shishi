# 第一轮验收（2026-09-16）

范围：侧栏与项目页的状态一致性，候选1.0.4 build5；主代理runtime self-review，sol/medium独立编写回归测试。

- G1 pass：侧栏已完成项目分组与条目均移除，日志簿与项目内容保留。
- G2 pass：沿用现有布局，侧栏小圆和标题大圆按同一比例绘制。
- G3 pass：合成项目左右同时6/10，点击完成同时7/10，CmdZ同时6/10；AX均提供一致进度标签。
- G4 pass（本次限定范围）：正式项目“语音识别方案”左右均1/1满圆，长备注限高不变；115项测试通过，Release构建/签名通过。0/部分/全满、任务完成/撤销/重做、归档保留和隐藏路由选择均有回归测试。

证据：当前会话CUA原生完整截图（合成项目6/10，正式项目1/1）及AX结果；为避免将真实库截图写入源码，仅保留会话证据。无本地截图文件，不伪造路径。窗口视口沿用用户当前尺寸，无布局尺寸改动。

自评分8.5，outcome accepted（仅本次限定范围），无未解决阻塞。未重跑全主题、VoiceOver和最小窗口视觉矩阵，不声明这些额外场景通过。
