# 设置UI决定

已读取settings契约v1。沿用Things原生工具栏/表单结构，用独立品牌名称和系统图标；增加数据页保留既有功能。

第一轮实机发现深色切换后slider无最小宽度被压缩，修正固定155pt滑杆与统一275pt下拉框；第二轮深色/20pt列表复验通过。项目图标环间隙跟随文字背景，避免深色白盘误导进度。主题仅真实改变时重绘，数据保存不重复切主题。

权限按钮不自动点击。未配置服务用说明页而非假开关。所有真实用户数据只读观察，状态变更使用QA库。截图保留在本轮CUA会话，不复制原应用/用户库截图到源码。
