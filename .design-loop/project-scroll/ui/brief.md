# 项目整页滚动修正

模式build，目标v1，用户2026-09-16最新Things截图覆盖此前备注160pt内部滚动要求。纯AppKit，保持已有排版、侧栏和固定底部工具栏。

契约：项目标题、备注、标题分组、待办及历史开关属于同一滚动文档；备注完整展开，不拥有独立滚动条，滚轮位于备注上仍滚整个项目。窗口尺寸不随文档增长；任务编辑及检查项聚焦保留，长备注下新增任务能自动定位到编辑位置。切路由回顶部，同项目普通刷新不跳动。

用户截图为冻结参考，不重新生成设计。现有布局由task-list header加bounded notes和独立table scroll构成，需调整容器，不改业务数据。主代理使用ui-loop检查截图，sol/medium子代理实现，主代理写回归和验收。合成库进行编辑，真实库仅观察。

验收矩阵：短/长/空备注、宽/窄窗口、整页滚动到末尾、最后任务编辑/新增、检查项聚焦、同页刷新与切页、历史展开、既有非项目列表。最多3轮实现复验；原版本验收文档保持历史，不重写为新行为。
