# 验证结果

self-review；实现已完成，运行时视觉验收待验证。

- `swift test`：212 项，1 项外部 Things 数据测试跳过，0 失败。
- `git diff --check`：通过。
- 新用例覆盖拖动排序、组内任务归属不变、撤销、非法位置与非法标题顺序不变更快照。
- G1/G2/G3/G4：not-tested（缺少本轮真实窗口操作与截图）。不以编译和控制器测试替代视觉验收。
- 未提交、未安装、未部署。

## 排序过渡补充

- 增加 220ms 原生行移动，尊重系统减少动态效果。
- 原生控件回归：分组控件复用且换位正确，选中标题保持；完整测试 213 项，1 跳过，0 失败。
- 真实鼠标拖放动画观感：not-tested。当前已运行的进程需重启才能加载本轮代码。

## 性能和效果修正最终验证

self-review。本轮真实截图：rounds/round-04/final-preview-down.png、rounds/round-04/final-after.png。原生窗口，浅色、合成库，1080x804 pt、2x 截图。基于 SwiftPM debug 可执行文件的当前未提交代码。

- 真实拖动：浅蓝浮层、阴影、浅灰落点占位；真实落下后标题完整显示，排序写入独立验收库，无残留空白。
- 性能用例：6000 行移动 2 行分组，旧路径 5998 次 moveRow，新路径 2 次。此证据为调用数量，不是 FPS 测量。
- `swift test`：218 项，1 项外部 Things 数据测试跳过，0 失败。
- 拖动图像蓝色渲染、缓存复用、源行恢复、多方向连续区块换位、非法序列回退、选中身份和撤销通过。
- `git diff --check` 通过；临时调试输出已移除。
- 大库真实帧率、深色主题和边缘自动滚动未进行运行时验收。未提交、未安装、未部署。
