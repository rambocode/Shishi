# 1.1.0 设置对齐验收

日期2026-09-16，build6，纯AppKit。对照本机Things设置窗口，只读观察，没有修改Things偏好或原数据库。

## 已实现

六页原生工具栏：常规、云同步能力说明、快速输入、提醒事项、日历、数据与备份。常规包含归档时机、Dock并集计数、外观、11–20pt文字大小、今天分组；默认值保留旧库行为。独立快速输入面板支持热键预设、目的地、取消保留草稿、失败保留、退出保护。日历/提醒接入EventKit，授权需用户点击，事件只读，提醒导入保留源且幂等。

## 自动验证

- 完整143项测试通过，0失败、0跳过，含授权Things导出副本只读兼容验证。
- 新增设置控件持久化、真实列表字号/分组、编辑器输入选区和first responder保持、归档恢复/重复导入/跨日父子关系、Dock去重、快速输入退出保护、fake系统权限/取消/重试/幂等测试。
- 独立sol/medium审查发现3项归档边界，均补充回归并修复：恢复备份先按当前策略处理pending、同源关闭对象合并保留本地pending、父项目归档同步处理关闭子任务。
- Release构建成功；本机临时签名校验成功，ZIP更新。

## 实际窗口验证

在独立验收库与QA应用中完成：

1. 六页可切换。外观切深色立即生效，重启保留深色与手动归档偏好。
2. 字号20时侧栏与任务文字同步变大，行高无裁切；恢复默认14。深色滑杆、底部工具栏及项目圆环背景已修正并截图复验。
3. 快速录入输入合成任务→取消→再次打开内容仍在→添加成功，读取验收JSON确认进入收件箱。
4. 手动归档模式完成合成任务后仍留原列表，点击归档后进入日志簿，pending清空。
5. 隐藏的快速录入草稿在退出时提示；Return默认取消退出，再选择保存并退出，磁盘确认只有成功保存后才退出。
6. 日历/提醒页显示尚未授权，无自动请求。未点击授权、未读取私人日历/提醒、未更改系统权限。
7. 数据与备份页保留导出、恢复、数据目录、Things导入入口。

正式应用已正常重启到1.1.0，设置窗口已打开。正式热键登记状态显示“已登记⌃⌥Space”。CUA模拟按键未触发Carbon全局回调，因此不把实际跨应用物理热键称为已实机通过；面板试用/保存链已验证。真实用户库没有执行完成、删除或测试写入。

## 未完成与限制

不是全部Things设置等价：Things Cloud与跨端同步、Spotlight、URL Scheme、快捷指令批量操作、自动填写、自动更新服务未接入。侧栏固定窗口宽度沿用现状，未提供切换行为。提醒事项为复制导入，不自动删除源；仅迁移标题/备注/截止。

系统授权成功后的真实账户读取与跨应用物理热键仍需用户环境验证，fake provider测试不能代替。未进行完整VoiceOver、全部窗口尺寸或真人可用性研究。未Developer ID公证、未提交或对外发布。
