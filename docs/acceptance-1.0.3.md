# 1.0.3 项目页与长备注验收

日期：2026-09-15；2026-09-16完成解锁后的最终实机验收。对应本轮三项用户要求：项目进度及录入项、项目更多菜单/底部入口、长备注限高滚动。保持纯 AppKit。

## 已实现与自动验证

- 真实项目进度圆环/内饼；已完成和已取消记录的显示/隐藏开关。
- 标题旁九项菜单：完成/重新打开、时间、标签、截止日期、移动、重复、复制、删除、分享。
- 默认底部五个图标：新建待办、新建标题、时间、移动、搜索；选中项的上下文工具栏继续保留。
- 简单项目重复与深复制，项目/标题/任务/检查项独立 ID，旧数据不变，失败原子回滚，单次撤销/重做。
- 原生分享选择器及草稿，不自动发送，排除废纸篓和内部重复模板。
- 项目头部备注最多160pt，短文收缩，长文完整滚动，宽度无效时不排版，重复布局复用测量结果。
- 全量 **111 项测试通过，0失败、0跳过**，包含授权 Things 导出副本的只读兼容测试。
- Release 构建通过；应用版本1.0.3（build4），本机临时签名校验通过。

## 已完成的实际窗口验证

使用独立合成验收库 `work/project-ui-verification.json`：

1. 180段长备注在固定备注区域滚动，从第1段滚动到第180段，下方待办仍可见，窗口未被撑高。
2. 项目4条开放待办、6条完成记录，显示60%进度与“显示6个录入项”。
3. 点击展开显示6条真实记录；完成一条开放待办变为7/10和7条记录，撤销后恢复6/10和6条。
4. 标题旁菜单的九项入口均可见；复制项目后打开独立副本，仍有4条开放、6条历史，原项目保留。
5. 从项目时间弹窗选择明天，副本进入计划。
6. 从重复弹窗选择每周并保存，独立验收库中规则持久化为week/1，固定周期模式。

独立验收副本初次通过LaunchServices读取合成JSON时阻塞在文件打开；没有完成UI初始化。结束该未加载数据的验收进程后，使用开发启动方式成功完成上述窗口验证。未将工具连接超时当作应用布局性能结论。

## 最终 Release 实机验收（2026-09-16）

解锁后将最终 Release 二进制复制到独立验收应用，正常重启后完成以下检查：

1. 完成配置每周重复的副本，旧项目进入已完成且旧规则被消费；只产生一个新项目，进度0/10。新项目及10条子任务开始日期均为2026-09-23，全部任务开放、全部检查项未完成，使用新的ID。
2. 菜单添加标签“界面验收”，保存并持久化；截止日期选择2026-09-17并持久化。
3. 移动到“无区域”，确认项目areaID为空且界面保持可用。
4. 原生分享选择器显示项目、待办及检查项草稿，随后取消，没有发送。
5. 删除项目弹出可恢复的废纸篓说明；按Return默认取消，项目未删除、废纸篓仍为0。实际软删除/恢复由现有自动测试覆盖，本轮未在GUI确认删除。
6. 正常退出旧正式应用并启动磁盘上的1.0.3（build4），保留原用户库和选中的“语音识别方案”。正式窗口显示新菜单、底部图标、160pt限高备注及可见滚动条；备注滚到底时下方待办仍可见。
7. 正式应用本机临时签名再次校验通过。111项测试与Release构建结果沿用同一最终代码的前轮验证，本次只更新验收文档，没有改动产品代码。

应用文件与压缩包已更新，正式窗口已运行新版。没有绕过锁屏，没有在真实用户库中执行完成/删除测试。交付仍为本机临时签名，未进行Developer ID公证或对外发布；不宣称与Things全部功能完全等价。
