# 1.0.4 侧栏项目一致性

日期：2026-09-16；纯AppKit，build5。

## 改动

- 侧栏项目、右侧标题、智能列表共用进度组件与状态配置：形状、颜色、比例同源，仅显示尺寸不同。
- 侧栏不显示已完成/取消项目及“已完成项目”分组；历史项目仍保存在日志簿。
- 打开不在侧栏中的历史项目时清除旧选择，防止错误指向另一项目。

## 验证

- sol/medium子代理新增4项回归，主代理审阅并运行全量115项测试，0失败。包含授权Things导出副本只读兼容验证。
- 实机合成库：左右6/10 → 完成待办后7/10 → 撤销后6/10；没有历史分组。
- 正式库只读观察：当前“语音识别方案”左右1/1，满圆一致；没有历史分组，长备注限高保持。
- Release构建、本机临时签名校验成功；正式窗口已正常重启到1.0.4，ZIP已更新。
- 未变更真实用户任务状态或删除数据；未提交、推送、公证或对外发布。
